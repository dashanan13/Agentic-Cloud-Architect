services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: a3-app
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      - AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT:-}
      - AZURE_OPENAI_KEY=${AZURE_OPENAI_KEY:-}
