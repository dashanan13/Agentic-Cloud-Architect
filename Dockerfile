# Multi-stage build placeholder for Azure Agentic Architect (A3)
# Update stages once frontend/backend services are scaffolded.
FROM alpine:3.21
CMD ["sh", "-c", "echo 'A3 container placeholder' && sleep infinity"]
